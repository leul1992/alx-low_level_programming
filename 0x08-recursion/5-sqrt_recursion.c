#include "main.h"
/**
 * _sqrt_recursion-return natural square root
 * @n:the number
 * Return:square root(success)
 * -1(failure)
 */
int _sqrt_recursion(int n)
{
	int prod = 1;

	if (prod > n)
		return (-1);
	else if (prod == 
}

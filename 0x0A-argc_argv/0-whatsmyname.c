#include <stdio.h>
#include <stdlib.h>
/**
 * main-print its name
 * @argc:argument count
 * @argv:argument of array pointer to char
 * Return (0);
 */
int main(int argc, char const *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}

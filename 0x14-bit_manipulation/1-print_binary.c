#inlcude "main.h"
/**
 * print_binary-prints the binary represenation of a number
 * @n: the number
 * Return: nothing
 */
void print_binary(unsigned long int n)
{
	int i = 0;
	char *c;

	while (n > 0)
	{

	}
}

#include <stdio.h>
#include <stdlib.h>
/**
 * main-print its name
 * @argc:argument count
 * @argv:argument of array pointer to char
 * Return (0);
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
